{
  "name": "zidicommerce",
  "private": true,
  "version": "0.1.0",
  "description": "ZidiCommerce monorepo",
  "scripts": {
    "dev:admin": "npm --workspace apps/admin run dev",
    "build:admin": "npm --workspace apps/admin run build",
    "test:api": "cd apps/api && go test ./..."
  },
  "workspaces": [
    "apps/admin",
    "packages/shared"
  ]
}

